module BCorrespondent.Api.Handler.Frontend.User.GetHistory (handle) where

import BCorrespondent.Transport.Response (Response)
import Katip.Handler (KatipHandlerM)

handle :: KatipHandlerM (Response ())
handle = undefined